require "spec_helper"

describe SnakyCamel do
  it "has a version number" do
    expect(SnakyCamel::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
